---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: networkinfos.crd.nsx.vmware.com
spec:
  group: crd.nsx.vmware.com
  names:
    kind: NetworkInfo
    listKind: NetworkInfoList
    plural: networkinfos
    singular: networkinfo
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkInfo is used to report the network information for a namespace.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          dnsDomains:
            items:
              description: DNSDomain defines the information for a DNS Domain.
              properties:
                domainName:
                  description: Domain name for configured in Zone.
                  type: string
                zoneType:
                  description: ZoneType describes the access type of the DNS domain.
                  enum:
                  - Private
                  - Public
                  type: string
              required:
              - domainName
              - zoneType
              type: object
            type: array
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          vpcs:
            items:
              description: VPCState defines information for VPC.
              properties:
                defaultSNATIP:
                  description: Default SNAT IP for Private Subnets.
                  type: string
                loadBalancerIPAddresses:
                  description: LoadBalancerIPAddresses (AVI SE Subnet CIDR or NSX
                    LB SNAT IPs).
                  type: string
                name:
                  description: VPC name.
                  type: string
                networkStack:
                  description: |-
                    NetworkStack indicates the networking stack for the VPC.
                    Valid values: FullStackVPC, VLANBackedVPC
                  enum:
                  - FullStackVPC
                  - VLANBackedVPC
                  type: string
                privateIPs:
                  description: Private CIDRs used for the VPC.
                  items:
                    type: string
                  type: array
              required:
              - defaultSNATIP
              - name
              type: object
            type: array
        required:
        - dnsDomains
        - vpcs
        type: object
    served: true
    storage: true
