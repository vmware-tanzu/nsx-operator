---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: ipblocksinfos.crd.nsx.vmware.com
spec:
  group: crd.nsx.vmware.com
  names:
    kind: IPBlocksInfo
    listKind: IPBlocksInfoList
    plural: ipblocksinfos
    singular: ipblocksinfo
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IPBlocksInfo is the Schema for the ipblocksinfo API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          externalIPCIDRs:
            description: |-
              ExternalIPCIDRs is a list of IPv4 CIDR strings. Each CIDR is a contiguous IP address
              space represented by network address and prefix length. The visibility of the
              IPBlocks is External. IPv6 CIDRs are listed in externalIPv6CIDRs.
            items:
              type: string
            type: array
          externalIPRanges:
            description: |-
              ExternalIPRanges is an array of contiguous IPv4 address space represented by start and end IPs.
              The visibility of the IPBlocks is External.
            items:
              properties:
                end:
                  description: The end IP Address of the IP Range.
                  type: string
                start:
                  description: The start IP Address of the IP Range.
                  type: string
              required:
              - end
              - start
              type: object
            type: array
          externalIPv6CIDRs:
            description: ExternalIPv6CIDRs is a list of IPv6 CIDR strings for external
              IP blocks (same visibility as externalIPCIDRs).
            items:
              type: string
            type: array
          externalIPv6Ranges:
            description: ExternalIPv6Ranges is an array of contiguous IPv6 address
              space for external IP blocks.
            items:
              properties:
                end:
                  description: The end IP Address of the IP Range.
                  type: string
                start:
                  description: The start IP Address of the IP Range.
                  type: string
              required:
              - end
              - start
              type: object
            type: array
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          privateTGWIPCIDRs:
            description: |-
              PrivateTGWIPCIDRs is a list of IPv4 CIDR strings. Each CIDR is a contiguous IP address
              space represented by network address and prefix length. The visibility of the
              IPBlocks is Private Transit Gateway. Only IPBlocks in default project will be included.
              IPv6 CIDRs are listed in privateTGWIPv6CIDRs.
            items:
              type: string
            type: array
          privateTGWIPRanges:
            description: |-
              PrivateTGWIPRanges is an array of contiguous IPv4 address space represented by start and end IPs.
              The visibility of the IPBlocks is Private Transit Gateway.
            items:
              properties:
                end:
                  description: The end IP Address of the IP Range.
                  type: string
                start:
                  description: The start IP Address of the IP Range.
                  type: string
              required:
              - end
              - start
              type: object
            type: array
          privateTGWIPv6CIDRs:
            description: PrivateTGWIPv6CIDRs is a list of IPv6 CIDR strings for private
              transit gateway IP blocks.
            items:
              type: string
            type: array
          privateTGWIPv6Ranges:
            description: PrivateTGWIPv6Ranges is an array of contiguous IPv6 address
              space for private transit gateway IP blocks.
            items:
              properties:
                end:
                  description: The end IP Address of the IP Range.
                  type: string
                start:
                  description: The start IP Address of the IP Range.
                  type: string
              required:
              - end
              - start
              type: object
            type: array
        type: object
    served: true
    storage: true
