---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: subnetipreservations.crd.nsx.vmware.com
spec:
  group: crd.nsx.vmware.com
  names:
    kind: SubnetIPReservation
    listKind: SubnetIPReservationList
    plural: subnetipreservations
    singular: subnetipreservation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The parent Subnet name of the SubnetIPReservation.
      jsonPath: .spec.subnet
      name: Subnet
      type: string
    - description: Number of IPs requested to be reserved.
      jsonPath: .spec.numberOfIPs
      name: NumberOfIPs
      type: string
    - description: List of reserved IPs.
      jsonPath: .status.ips[:]
      name: IPs
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SubnetIPReservation is the Schema for the subnetipreservations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SubnetIPReservationSpec defines the desired state of SubnetIPReservation
            properties:
              ipAddressType:
                description: IPAddressType defines the IP address type of the SubnetIPReservation.
                enum:
                - IPV4
                - IPV6
                - IPV4IPV6
                type: string
                x-kubernetes-validations:
                - message: Value is immutable
                  rule: self == oldSelf
              numberOfIPs:
                description: NumberOfIPs defines number of IPs requested to be reserved.
                maximum: 100
                minimum: 1
                type: integer
                x-kubernetes-validations:
                - message: numberOfIPs is immutable
                  rule: self == oldSelf
              reservedIPs:
                description: |-
                  ReservedIPs represents array of Reserved IPs. It can can contain IP addresses,
                  IP Address range and CIDRs.
                  Supported formats include: ["192.168.1.1", "192.168.1.3-192.168.1.100", "192.168.2.0/28",
                  "2001:db8::1", "2001:db8::1-2001:db8::ff", "2001:db8::1/64"]
                items:
                  type: string
                minItems: 1
                type: array
              subnet:
                description: |-
                  Subnet specifies the Subnet to reserve IPs from.
                  The Subnet needs to have static IP allocation activated.
                type: string
                x-kubernetes-validations:
                - message: subnet is immutable
                  rule: self == oldSelf
            required:
            - subnet
            type: object
            x-kubernetes-validations:
            - message: Only one of numberOfIPs or reservedIPs can be specified
              rule: '!has(self.numberOfIPs) || !has(self.reservedIPs)'
            - message: One of numberOfIPs or reservedIPs must be specified
              rule: has(self.numberOfIPs) || has(self.reservedIPs)
            - message: reservedIPs cannot be unset once set
              rule: '!has(oldSelf.reservedIPs) || has(self.reservedIPs)'
            - message: numberOfIPs cannot be unset once set
              rule: '!has(oldSelf.numberOfIPs) || has(self.numberOfIPs)'
            - message: ipAddressType can only be set when numberOfIPs is specified
              rule: '!has(self.ipAddressType) || has(self.numberOfIPs)'
          status:
            description: SubnetIPReservationStatus defines the observed state of SubnetIPReservation
            properties:
              conditions:
                description: |-
                  Conditions described if the SubnetIPReservation is configured on NSX or not.
                  Condition type ""
                items:
                  description: Condition defines condition of custom resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: Message shows a human-readable message about condition.
                      type: string
                    reason:
                      description: Reason shows a brief reason of condition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type defines condition type.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              ips:
                description: |-
                  List of reserved IPs.
                  Supported formats include: ["192.168.1.1", "192.168.1.3-192.168.1.100", "192.168.2.0/28"]
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.subnet
    served: true
    storage: true
    subresources:
      status: {}
