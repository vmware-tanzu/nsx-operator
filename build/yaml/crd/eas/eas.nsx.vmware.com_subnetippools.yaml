---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: subnetippools.eas.nsx.vmware.com
spec:
  group: eas.nsx.vmware.com
  names:
    kind: SubnetIPPools
    listKind: SubnetIPPoolsList
    plural: subnetippools
    singular: subnetippools
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Display name of the IP pool
      jsonPath: .spec.displayName
      name: DisplayName
      type: string
    - description: Total IPs in pool
      jsonPath: .spec.poolUsage.totalIps
      name: TotalIPs
      type: integer
    - description: Available IPs in pool
      jsonPath: .spec.poolUsage.availableIps
      name: AvailableIPs
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SubnetIPPools is the Schema for the subnet IP pools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SubnetIPPoolSpec represents an IP pool associated with a
              subnet.
            properties:
              displayName:
                description: Display name of the IP pool.
                type: string
              id:
                description: ID of the IP pool.
                type: string
              path:
                description: Path of the IP pool.
                type: string
              poolSubnets:
                description: IP pool subnets (CIDR ranges).
                items:
                  description: SubnetIPPoolSubnet represents a subnet within an IP
                    pool.
                  properties:
                    allocationRanges:
                      description: IP ranges in the subnet.
                      items:
                        description: IpPoolRange represents an IP range with start
                          and end addresses.
                        properties:
                          end:
                            description: The end IP address of the range.
                            type: string
                          start:
                            description: The start IP address of the range.
                            type: string
                        required:
                        - end
                        - start
                        type: object
                      type: array
                    cidr:
                      description: CIDR of the subnet.
                      type: string
                    gatewayIp:
                      description: Gateway IP.
                      type: string
                  type: object
                type: array
              poolUsage:
                description: IP pool usage statistics.
                properties:
                  allocatedIpAllocations:
                    description: Number of allocated IP allocations.
                    format: int64
                    type: integer
                  availableIps:
                    description: Number of available IPs.
                    format: int64
                    type: integer
                  requestedIpAllocations:
                    description: Number of requested IP allocations.
                    format: int64
                    type: integer
                  totalIps:
                    description: Total number of IPs in the pool.
                    format: int64
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
