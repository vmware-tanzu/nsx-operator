---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: vpcipaddressusages.eas.nsx.vmware.com
spec:
  group: eas.nsx.vmware.com
  names:
    kind: VPCIPAddressUsage
    listKind: VPCIPAddressUsageList
    plural: vpcipaddressusages
    singular: vpcipaddressusage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: VPC name
      jsonPath: .metadata.name
      name: VPC
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VPCIPAddressUsage is the Schema for the VPC IP address usage
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VPCIPAddressUsageSpec represents the IP address usage of
              a VPC.
            properties:
              ipBlocks:
                description: VPC ip address blocks.
                items:
                  description: VpcIpAddressBlock represents IP block usage for a VPC.
                  properties:
                    allocatedByVpc:
                      description: Resources allocated by VPC.
                      properties:
                        accessMode:
                          description: Access mode of VPC subnet.
                          type: string
                        count:
                          description: Count of used resources by VPC on IP block.
                          format: int64
                          type: integer
                        ipAddresses:
                          description: IP addresses allocated by VPC.
                          items:
                            description: VpcIpAddress represents a VPC IP address
                              entry.
                            properties:
                              ipAddress:
                                description: IP address allocated.
                                type: string
                            type: object
                          type: array
                        percentageUsed:
                          description: Percentage of used IP address resources by
                            VPC.
                          type: string
                      type: object
                    available:
                      description: Available IP address space.
                      format: int64
                      type: integer
                    cidrs:
                      description: CIDR list for IP block.
                      items:
                        type: string
                      type: array
                    excludedIps:
                      description: Excluded IP address ranges.
                      items:
                        description: IpPoolRange represents an IP range with start
                          and end addresses.
                        properties:
                          end:
                            description: The end IP address of the range.
                            type: string
                          start:
                            description: The start IP address of the range.
                            type: string
                        required:
                        - end
                        - start
                        type: object
                      type: array
                    path:
                      description: Path for IP block.
                      type: string
                    percentageUsed:
                      description: Percentage of used IP address space.
                      type: string
                    ranges:
                      description: IP address ranges.
                      items:
                        description: IpPoolRange represents an IP range with start
                          and end addresses.
                        properties:
                          end:
                            description: The end IP address of the range.
                            type: string
                          start:
                            description: The start IP address of the range.
                            type: string
                        required:
                        - end
                        - start
                        type: object
                      type: array
                    total:
                      description: Total ip address space.
                      format: int64
                      type: integer
                    visibility:
                      description: Visibility of IP block.
                      type: string
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}