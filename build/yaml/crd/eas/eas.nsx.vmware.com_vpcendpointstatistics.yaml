---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: vpcendpointstatistics.eas.nsx.vmware.com
spec:
  group: eas.nsx.vmware.com
  names:
    kind: VPCEndpointStatistics
    listKind: VPCEndpointStatisticsList
    plural: vpcendpointstatistics
    singular: vpcendpointstatistics
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VPCEndpointStatistics represents the statistics of a VPC Endpoint.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          rx:
            description: Statistics contains the traffic statistics for a VPC service
              endpoint or VPC endpoint.
            properties:
              droppedPackets:
                format: int64
                type: integer
              totalBytes:
                format: int64
                type: integer
              totalPackets:
                format: int64
                type: integer
            type: object
          tx:
            description: Statistics contains the traffic statistics for a VPC service
              endpoint or VPC endpoint.
            properties:
              droppedPackets:
                format: int64
                type: integer
              totalBytes:
                format: int64
                type: integer
              totalPackets:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
