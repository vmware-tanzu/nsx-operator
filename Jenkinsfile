pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Your build steps here
                sh 'echo "Building pull request"'
            }
        }
        stage('Test') {
            steps {
                // Your test steps here
                sh 'echo "Testing pull request"'
            }
        }
    }
}
