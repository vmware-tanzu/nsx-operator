name: Makefile CI

on:
  push:
    branches: [ v4.1.1 ]
  pull_request:
    branches: [ v4.1.1 ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.19
        
    - name: Run build
      run: make build

    - name: Run test
      run: make test
